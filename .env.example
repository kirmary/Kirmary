# Optional RFQ delivery endpoint; configure before production.
RFQ_ENDPOINT=

# Connect a PUBLIC Vercel Blob store to the Vercel project, Production scope.
# Server-only secret. Never prefix with NEXT_PUBLIC_ and never commit its value.
# Required for production uploads; optional locally (without it, dev uses public/).
BLOB_READ_WRITE_TOKEN=
